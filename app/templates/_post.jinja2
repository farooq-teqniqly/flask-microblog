<table>
        <tr valign="top">
            <td><img src="{{ post.author.get_gravatar(36) }}" alt=""></td>
            <td>{{ post.author.username }} says: <br>{{ post.body }}</td>
        </tr>
</table>